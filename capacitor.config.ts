import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todasaferide.app',
  appName: 'TODA Ride Mobility',
  webDir: 'dist'
};

export default config;
